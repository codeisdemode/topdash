# Server Monitoring Agent Configuration

# Required: Server ID from the dashboard
SERVER_ID=your-server-uuid-here

# Required: API token from server registration
API_TOKEN=your-agent-token-here

# API endpoint (default: http://localhost:3001)
API_URL=http://your-api-domain.com:3001

# Metrics collection interval in seconds (default: 60)
INTERVAL=60

# Optional: Site URL to monitor for HTTP status
SITE_URL=https://your-website.com

# Optional: Server name for identification
SERVER_NAME=Production Server 1